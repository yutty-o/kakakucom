<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kakakucom.repository.AccountRepository">

    <select id="findOne" resultType="kakakucom.model.User">
      SELECT
          user_id,
          name,
          email,
          password
      FROM
          users
      WHERE
          is_deleted = 0
          AND email = '${email}'
    </select>

    <insert id="create">
      INSERT INTO
          users (
              name,
              email,
              password
          )
      VALUES (
          #{user.name},
          #{user.email},
          #{user.password}
        )
    </insert>

</mapper>