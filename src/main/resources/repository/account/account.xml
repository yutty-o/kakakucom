<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kakakucom.repository.AccountRepository">

    <insert id="create">
      INSERT INTO
        users (
          name,
          email,
          password,
          is_deleted
        )
      VALUES (
        #{user.name},
        #{user.email},
        #{user.password},
        #{user.isDeleted}
      )
    </insert>

</mapper>